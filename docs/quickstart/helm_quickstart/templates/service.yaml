apiVersion: v1
kind: Service
metadata:
  name: {{ include "tornjak_quickstart.fullname" . }}
  labels:
    {{- include "tornjak_quickstart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tornjak_quickstart.selectorLabels" . | nindent 4 }}
